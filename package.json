{
  "name": "aopctv",
  "version": "0.0.0",

  "homepage": ".",

  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },

  "dependencies": {},
  "devDependencies": {
    "gh-pages": "^6.0.0"
  }
}
